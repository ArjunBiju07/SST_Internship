const readline=require("readline").createInterface({
    input:process.stdin,
    output:process.stdout,
});
readline.question("Enter the size of the array: ",(a)=>{
    readline.question("Enter the values: ",(b)=>{
        
        let arr=new Array(a);
        
        for(let i=0;i<a;i++){

            arr.push(sum)

      }



        readline.close();
    });
});